# Shared configuration

This package boundary is reserved for shared typed configuration as implementation grows. Sprint 1 keeps the active base TypeScript, formatter, test, and CI configuration at the repository root for tool compatibility.
