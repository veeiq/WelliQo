# Supabase configuration boundary

Sprint 1 establishes the Supabase integration boundary only. No database tables, migrations, seeds, policies, or business data exist yet.

When a Supabase project is provisioned, place public project configuration in `.env.local` using `.env.example` as the template. Never commit service-role keys or real credentials.
