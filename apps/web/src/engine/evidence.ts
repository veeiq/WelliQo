import { ClinicalEvidence } from '../clinical/evidence';

/**
 * Utility class for constructing traceable evidence trails.
 */
export class EvidenceBuilder {
  public static build(questionIds: string[], answers: any, metrics: any): ClinicalEvidence {
    return null as any; // To be implemented
  }
}
